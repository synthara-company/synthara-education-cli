#!/bin/bash
# Simple wrapper script for Gemini Chat

# Activate virtual environment if it exists
if [ -d "venv" ]; then
    source venv/bin/activate
fi

# Run the Gemini Chat script
python3 simple_gemini_cli/gemini_chat.py "$@"
